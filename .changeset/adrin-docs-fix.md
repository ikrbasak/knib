---
'@knib/adrin': patch
---

Fix the README's Development section to reference the package's actual
scripts (`pnpm tsc`, `pnpm build`) instead of nonexistent `pnpm dev` and
`pnpm typecheck` scripts.
